extends ../layout

block content
    h3= book.name
    a(href=`/books/edit/${book.id}`) Edit Book
    div
        p Author: #{book.author}
        p Publisher: #{book.publisher}
        p Genre: #{book.genre}
        p Pages: #{book.numOfPages}
        p Cover: #{book.imageOfCover}
        p= book.description